server:
    port: 8080

logging.level.org.springframework.security: DEBUG

spring:
  application:
    name: ms-duminjum
    instance_id: 002
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://115.28.36.79:3306/aixuManager?characterEncoding=utf-8
    username: aixuManager
    password: aixuManager1qaz@WSX
  #数据库连接池配置
    max-active: 200
    max-idle: 100
    min-idle: 50
    initial-size: 20
    test-on-borrow: false
    test-on-return: false
    test-while-idle: true
    validation-query: select 1
    #每10分钟收集空闲连接，单位毫秒
    time-between-eviction-runs-millis: 600000
    #连接的最大空闲时间，单位毫秒，60分钟
    min-evictable-idle-time-millis: 3600000
    max-wait: 60000

spring.redis.host: 139.196.12.123
spring.redis.port: 6379
spring.redis.password: redis@QWASZX
spring.redis.pool.max-active: 2000
spring.redis.pool.max-wait: 10000
spring.redis.pool.max-idle: 500
spring.redis.timeout: 30000

logging:
  file: e://logs/ms-duminjun.log
  level.*: INFO

mybatis.config: classpath:mybatis-config.xml